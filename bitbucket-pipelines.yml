pipelines:
  branches:
    master:
      - step:
          name: Push to GitHub Repository
          script:
            - git remote add github https://${GITHUB_USERNAME}:${GITHUB_TOKEN}@github.com/username/repository.git
            - git push github master
          services:
            - docker